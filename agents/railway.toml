[build]
builder = "nixpacks"

[deploy]
startCommand = "python meTTa_eval_agent_refactored.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[build.nixpacks]
providers = ["python"]
